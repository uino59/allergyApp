import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({

    homeButton: {
        alignItems: 'center',
        justifyContent: 'center',
        padding: 20,
        backgroundColor: '#fff',
        margin: 5,
        borderRadius: 20,
    },
    buttonText: {
    fontSize: 21,
    margin: 10,
    textAlign: 'center',
    }
})